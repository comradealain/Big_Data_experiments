#include <iostream>
#include <map>
#include <vector>
#include <initializer_list>

using namespace std;

int main ()
{
  map<int,vector<int> > m;
/*
  first['a']=10;
  first['b']=30;
  first['c']=50;
  first['d']=70;

  cout<<first['a'];
  
*/  

//m	[0] = {2,1,0} ;
m[0].push_back(2);
cout<<m[0][0];
 return 0;
 
 }
